import { FC } from 'react';

import { ITutiScrollAnimateProps } from './interface';

const TutiScrollAnimate: FC<ITutiScrollAnimateProps> = ({
  children,
  itemWidth,
  itemsToShow,
}: ITutiScrollAnimateProps): number => {
  return 0;
};
